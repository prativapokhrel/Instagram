.col-md-6.center-div
  = form_for(@picture, :as => :picture, :url => pictures_path, :html => { :multipart => true }) do |f|

    .form-group
      = f.label "Hazard Type:"
      = f.file_field :image, class: 'form-control', value: 'landslide'